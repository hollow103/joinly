# Environment and secrets
.env
.env.*
!.env.example
!mobile/.env.example

# JavaScript and React
mobile/node_modules/
admin/node_modules/
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/expo-env.d.ts
mobile/*.tsbuildinfo
admin/dist/

# Generated native projects (expo prebuild)
mobile/ios/
mobile/android/

# Local Docker Compose overrides (per-machine, not shared)
compose.override.yaml
compose.override.yml

# Logs and operating system files
*.log
.DS_Store

# Local OpenCode configuration and session state
.opencode/
opencode.json
AGENTS.md
SESSION_HANDOF.md

# Local Claude Code configuration (skills mirrored from .agents/, MCPs live in ~/.claude.json)
.claude/skills/
.playwright-mcp/

# Claude Code tooling mirrored into the working tree (not part of the app)
.agents/
skills-lock.json
/preview-full.png
/canvas-resumen.png

# Design-canvas seeded output bundles (regenerable from the .dc.html sources)
mobile/rebranding/joinly-rebranding.html
mobile/rebranding/navegables/joinly-radar-navegable.html
mobile/rebranding_beta/joinly-rebranding-beta.html
