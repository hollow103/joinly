node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# env files — never commit; keep only the checked-in template
.env
.env.*
!.env.example

# editor / tooling
.vscode/*
!.vscode/extensions.json
.DS_Store
*.log
